# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RTH' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RTH
  pod 'Alamofire', '~> 4.5'
  pod 'Charts', '~> 3.0'

  target 'RTHTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RTHUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
